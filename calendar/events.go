package calendar

// Event represents a calendar event
type Event struct {
	// StartTime specifies the time, in epoch, that determines when this event will start
	StartTime int64

	// EndTime specifies the time, in epoch, that determines when this event will start
	EndTime int64
}
